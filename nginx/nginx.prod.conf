events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;

    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript application/wasm;
    gzip_min_length 256;
    gzip_comp_level 6;
    gzip_proxied any;
    gzip_vary on;

    server {
        listen 80;
        server_name localhost; #www.genamshen.com genamshen.com;

        # Redirect all HTTP requests to HTTPS
        return 301 https://$host$request_uri;
    }

    server {
        listen 443 ssl; # Change to listen on port 443 for SSL
        server_name localhost;

        ssl_certificate /etc/nginx/localhost.crt; # Full path to the certificate
        ssl_certificate_key /etc/nginx/localhost.key; # Full path to the key

        root /usr/share/nginx/html;
        index index.html;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
